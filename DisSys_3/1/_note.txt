Question:
  Try out the Python TCP/UDP client-server and
  multithreaded server examples. Correct any problem.

Ans:
  The only problem is it never ends, so I make it end when the client
  sends 'END', but only for the client and it's thread.